(function (angular, _) {
    "use strict";

    angular.module("madeasy.parser.controllers.book",[])

    .controller("madeasy.parser.controllers.book", Book);
    Book.$inject = [
        "$scope", "$state"
    ];
    function Book ($scope, $state) {

    }
})(window.angular, window._);
